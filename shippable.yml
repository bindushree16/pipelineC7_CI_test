language: python

python:
  - 2.7 #.5 
  - 3.4 #.8 
  - 3.5 #.5 
  - 3.6 #.5
  
build:
  pre_ci_boot:
    image_name: drydock/c7pytall
    image_tag: master
    pull: true
    
  ci:
    - python --version 
    - pip --version #8.1.2
    - virtualenv --version #15.1.0
